@model List<Depom.Application.Category.DTOs.CategoryListDto>
@{
    ViewData["Title"] = "Kategoriler";
}

<div class="d-flex align-items-center justify-content-between mb-4">
    <div>
        <h5 class="page-title">Kategoriler</h5>
        <p class="page-subtitle">Urun kategori listesi</p>
    </div>
    <a asp-action="Create" class="btn-depom-primary">
        <i class="ri-add-line me-1"></i> Yeni Kategori
    </a>
</div>

<div class="card">
    <div class="card-body p-0">
        <table class="table mb-0">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Kategori Adi</th>
                    <th>Durum</th>
                    <th class="text-end">Islemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-depom-muted" style="font-size:13px;">@item.Id</td>
                        <td class="fw-semibold">@item.Name</td>
                        <td>
                            @if (item.IsActive)
                            {
                                <span class="badge-depom-success">Aktif</span>
                            }
                            else
                            {
                                <span class="badge-depom-danger">Pasif</span>
                            }
                        </td>
                        <td class="text-end">
                            <a asp-action="Edit" asp-route-id="@item.Id"
                               class="btn btn-sm btn-outline-primary me-1">
                                <i class="ri-pencil-line"></i>
                            </a>
                            <button type="button" class="btn btn-sm btn-outline-danger"
                                    data-bs-toggle="modal" data-bs-target="#deleteModal"
                                    data-id="@item.Id" data-name="@item.Name">
                                <i class="ri-delete-bin-line"></i>
                            </button>
                        </td>
                    </tr>
                }
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="4" class="text-center text-depom-muted py-4">
                            Henuz kategori eklenmemis.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h6 class="modal-title">Kategoriyi Sil</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p><strong id="deleteName"></strong> kategorisini silmek istediginize emin misiniz?</p>
                <p class="text-danger" style="font-size:13px;">Bu islem geri alinamaz.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Iptal</button>
                <form id="deleteForm" method="post">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-danger btn-sm">Sil</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('deleteModal').addEventListener('show.bs.modal', function (e) {
            const btn = e.relatedTarget;
            document.getElementById('deleteName').textContent = btn.getAttribute('data-name');
            document.getElementById('deleteForm').action = '/Category/Delete/' + btn.getAttribute('data-id');
        });
    </script>
}